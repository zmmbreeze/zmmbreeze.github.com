<!doctype html>
<html>
<head>
    <meta charset="utf-8"/>
    <title>MZhou's blog</title>
    <link rel="stylesheet" href="./assets/index.css" />
    <link rel="stylesheet" href="./assets/entry.css" />
    <link rel="stylesheet" href="./assets/highlight.css" />
</head>
<body>
    <div class="entry">
        {{{data.content}}}
    </div>
    <div class="date">
        {{data.mtime}} @MZhou
    </div>
    <div class="others">
        {{#all}}
        <a href="{{destpath}}">{{filename}}</a>
        {{/all}}
    </div>
    <!-- 这是一段隐藏文字，包含的文字原本放于css content中。放在这里是只为了让font-spider能爬到字 -->
    <span style="position:absolute;left:-9999px;">、《》</span>
</body>
</html>
